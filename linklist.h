struct list* createlist(struct list*);  #如果在前面主程式main有輸入資料的話在改成struct list*int  char
struct list* Add(struct list*);         #如果在前面主程式main有輸入資料的話在改成struct list*int  char
struct list* Delete(struct list*, int);
struct list* Show(struct list*);
